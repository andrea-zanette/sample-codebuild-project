version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
  build:
    commands:
      - echo "Running tests..."
      - npm test
      - echo "Build completed"

artifacts:
  files:
    - src/**/*
    - package.json
